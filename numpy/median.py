#calculate median
# Replace ___ with your code
import numpy as np

# take array as user input
marks = np.array(input().split(),dtype = int)

# compute the median of marks
median=np.median(marks)

# print the median
print(median)